# تقرير المشروع (نسخة محدّثة): دفتري — Vite + SolidJS

النسخة دي بناء فوق نفس تصميم "دفتري" (لاندنج بيدج فوترة للفريلانسرز)، بس بـ stack تقني أحدث حسب طلبك. الهدف إن البورتفوليو يوريك عارف تشتغل بأدوات 2026 مش بس HTML/CSS/JS تقليدي.

## 1) إيه اللي اتغيّر ولية

### أ) نظام البناء: Vite بدل ما فيه أدوات بناء أصلًا
النسخة الأولى مكنش فيها build step خالص (ملفات ثابتة). دلوقتي المشروع بقى **مشروع Vite حقيقي**:
- `npm run dev` بيفتح dev server فيه **HMR** (Hot Module Replacement) — أي تعديل في الكود بيظهر في المتصفح فورًا من غير ما الصفحة تعمل reload كامل.
- الـ dev server وTransform بيستخدموا **esbuild** (لغة Go) اللي أسرع بعشرات المرات من Webpack التقليدي.
- الـ build النهائي (`npm run build`) بيستخدم **Rollup** وبيطلع ملفات مضغوطة ومقسّمة (code-splitting) جاهزة للإنتاج.
- **ليه Vite تحديدًا مش Turbopack؟** Turbopack لسه مربوط بمشاريع Next.js بشكل أساسي، وVite هو المعيار الأوسع انتشارًا ومتوافق مع أي framework (React, Solid, Vue, Svelte)، وده بيخليه اختيار أذكى لمشروع مستقل زي ده.

### ب) SolidJS بدل الـ Vanilla JS — كمثال حي على Signal-based state
كل حاجة كانت بتتغيّر بـ `document.querySelector` و`textContent` يدويًا (تبديل اللغة، عداد الإحصائيات، الفورم) بقت مبنية بـ **Signals**:
```js
const [lang, setLang] = createSignal('ar');
```
الفرق العملي: لما `lang` يتغيّر، Solid بيحدّث **بالظبط العناصر اللي بتستخدم القيمة دي** في الـ DOM، من غير ما يعيد بناء الصفحة كلها أو حتى يعمل diff زي React. ده أسرع وأخف، وهو بالظبط اللي بيميّز SolidJS وQwik وSvelte 5 عن جيل React/Vue التقليدي.

**ليه SolidJS تحديدًا وماخترتش React أو Svelte 5؟**
- React لسه بيعتمد على Virtual DOM حتى مع React Compiler الجديد، فمش أنقى مثال لفكرة الـ Signals.
- Svelte 5 ممتاز برضه، بس بيحتاج compiler خاص (`.svelte` files) بيغيّر شكل الكود بالكامل.
- SolidJS بيدّيك JSX عادي (قريب من React اللي انت متعوّد عليه) بس بمحرك Signals تحتيه، فبيبقى أسهل مرحلة انتقال ليك كمطوّر جاي من React/Vanilla.

### ج) Full-stack بسيط: Serverless Function بدل ما الفورم يفضل بدون باك إند
ضفت `api/subscribe.js` — دالة Serverless بتشتغل تلقائي لو المشروع اترفع على Vercel، وبتستقبل طلب POST من فورم الاشتراك، تتحقق من الإيميل **تاني من جهة السيرفر**، وترجّع نتيجة.

**ليه مش Next.js App Router أو React Server Components كاملة؟**
RSC ومفاهيم زي Server Components بتاخد قيمتها الحقيقية في تطبيقات فيها **بيانات ديناميكية من قاعدة بيانات** بتتغيّر مع كل مستخدم (dashboard، صفحة منتج بسعر متغيّر، إلخ). صفحة هبوط ثابتة زي دي مش محتاجة SSR كامل — هتبقى بس overhead من غير فايدة حقيقية. البديل العملي والصح هندسيًا هنا هو **static site + serverless function واحدة** للجزء الوحيد اللي فعلاً محتاج سيرفر (استقبال الإيميل). ده بالظبط النمط اللي بتشوفه في مشاريع حقيقية كتير (Jamstack + serverless function).

> لو عايز مشروعك الجاي في البورتفوليو يوري RSC / Next.js App Router فعليًا، الأنسب إنه يكون **dashboard أو تطبيق فيه بيانات حقيقية** (مثلاً: لوحة تحكم بتجيب بيانات من API) مش لاندنج بيدج — أنا مستعد أبنيلك واحد كمشروع تاني في القايمة بتاعتك.

### د) CSS حديث بدل أي مكتبة خارجية
- **Container Queries** (`container-type: inline-size`): شبكة المميزات (`.feature-grid`) بقت بتاخد قرار تخطيطها بناءً على **مساحة الحاوية نفسها**، مش عرض الشاشة. لو يومًا حطيت نفس البطاقات جوه sidebar ضيق، هتترتب صح تلقائيًا من غير أي media query إضافية.
- **`:has()` selector**: فورم الاشتراك (`.signup-form`) بقى بيغيّر لون حدوده لما الإنبوت اللي جواه يبقى فيه focus أو error — من غير أي JS بيضيف/يشيل class. الحاوية "حسّت" بحالة العنصر اللي جواها مباشرة من CSS.
- **View Transitions API**: تبديل اللغة (AR/EN) بقى بيتنادي جوه `document.startViewTransition()`، فالمتصفح بيعمل crossfade ناعم بين الحالتين تلقائيًا. المتصفحات اللي مش بتدعمها (Safari/Firefox القديمة) بترجع للتبديل الفوري العادي من غير أي كسر.

## 2) إيه اللي فضل زي ما هو (عن قصد)
- **الهوية البصرية** (الألوان الغامقة + الذهبي + عنصر الليدجر المتحرك) فضلت زي ما هي — التقنية اتغيّرت، مش التصميم.
- **الأمان**: نفس مبادئ CSP، عدم استخدام `eval`/`innerHTML` لبيانات المستخدم، `rel="noopener noreferrer"`، بالإضافة دلوقتي لتحقق حقيقي من السيرفر في `api/subscribe.js`.
- **إمكانية الوصول**: skip link، `aria-live`، `prefers-reduced-motion` — كلهم موجودين برضه، وView Transitions بتحترم `prefers-reduced-motion` كمان.

## 3) عن "الذكاء الاصطناعي المدمج" (AI-Driven UI Tools)
النقطة دي مش تقنية بتتحط "جوه" المنتج نفسه، هي بالأساس **جزء من طريقة بناء المشروع مش من مخرجاته**. يعني: أدوات زي Cursor وGitHub Copilot وv0.dev هي اللي بتساعدك تكتب الكود (وده بالظبط اللي إحنا بنعمله دلوقتي — "Vibe Coding" اللي بتستخدمه)، مش feature بتحطها جوه لاندنج بيدج فوترة. لو حبيت تضيف feature ذكاء اصطناعي حقيقي **جوه المنتج نفسه** (مثلاً: مساعد ذكي يجاوب على أسئلة العميل عن التسعير)، ده هيحتاج مفتاح API ومنطق سيرفر منفصل — أنا مستعد أضيفه كمشروع بورتفوليو منفصل يوريك تكامل AI حقيقي (زي `ai/react` من Vercel) بدل ما نحشره في صفحة هبوط بسيطة.

## 4) التحقق من سلامة الكود
اتعمل build حقيقي (`npm run build`) والمشروع بيتبني من غير أي أخطاء، وكل مفاتيح الترجمة (AR/EN) اتفحصت برمجيًا للتأكد إنها متطابقة بين المكوّن وقاموس الترجمة.

## 5) الخطوات الجاية
1. `npm install` ثم `npm run dev` للمعاينة محليًا.
2. ادفع المشروع على GitHub repo جديد (اسم واضح: `daftari-landing`).
3. اربطه بـ Vercel — هيكتشف إعدادات Vite تلقائيًا.
4. حط رابط الـ live demo ورابط الـ GitHub جنب بعض في بروفايلك على خمسات/مستقل، واكتب في وصف المشروع: "Vite + SolidJS + Serverless function" — الكلمات دي بالظبط بتفرق مع عملاء تقنيين بيدوروا على حد مواكب.

---
لو عايز الخطوة الجاية تكون مشروع تاني في القايمة (لوحة تحكم فيها بيانات حقيقية توريك RSC/Server Components بجد)، قولّي وهنبدأ فيه بنفس الطريقة.
